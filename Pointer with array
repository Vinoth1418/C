#include <stdio.h>
int main()
{
  int arr[3]={1,2,3};
  int *p=arr;
  int len=sizeof(arr)/sizeof(arr[0]);
  printf("Length of the array:%d\n",len);
  for(int i=0;i<len;i++){
  printf("%d\n",p[i]);
}
return 0;
}
